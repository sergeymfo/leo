# Leo Donation Web App

Telegram Web App для прийому донатів через Buy Me a Coffee.

## Швидкий старт

### 1. Налаштування Buy Me a Coffee

1. Зареєструйтесь на https://www.buymeacoffee.com/
2. Виберіть свій username (наприклад: `serhii`)
3. Налаштуйте профіль та підключіть спосіб виведення коштів

### 2. Налаштування Web App

Відредагуйте файл `script.js`, знайдіть розділ `CONFIG` і змініть:

```javascript
const CONFIG = {
    bmcUsername: 'serhii', // ⬅️ ЗАМІНІТЬ на ваш username
    basePrice: 1,          // Базова ціна на BMC
    minAmount: 10,         // Мінімальна сума
    maxAmount: 10000,      // Максимальна сума
    currency: 'UAH'        // Валюта
};
```

### 3. Деплой на GitHub Pages

1. Завантажте всі файли в репозиторій
2. Перейдіть в **Settings → Pages**
3. Виберіть **Source**: Deploy from a branch
4. Виберіть **Branch**: `main`, папка `/ (root)`
5. Збережіть

Ваш Web App буде доступний за адресою:
```
https://sergeymfo.github.io/leo/
```

### 4. Інтеграція в Telegram бот

#### Варіант 1: Inline кнопка

```python
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton, WebAppInfo

WEBAPP_URL = "https://sergeymfo.github.io/leo/"

keyboard = InlineKeyboardMarkup(inline_keyboard=[
    [InlineKeyboardButton(
        text="💰 Підтримати Leo",
        web_app=WebAppInfo(url=WEBAPP_URL)
    )]
])

await message.answer("Виберіть суму:", reply_markup=keyboard)
```

#### Варіант 2: Через @BotFather

1. Знайдіть @BotFather в Telegram
2. Відправте `/setmenubutton`
3. Виберіть свій бот
4. Введіть текст: `💰 Підтримати`
5. Введіть URL: `https://sergeymfo.github.io/leo/`

## Як це працює

1. Користувач відкриває Web App через Telegram бот
2. Вибирає суму або вводить власну
3. Натискає "Підтримати проект"
4. Відкривається Buy Me a Coffee з підготовленою сумою
5. Завершує оплату на BMC

## Структура файлів

```
leo/
├── index.html      # Головна сторінка Web App
├── styles.css      # Стилі та дизайн
├── script.js       # Логіка та інтеграція BMC
├── README.md       # Ця інструкція
└── DEPLOY_GUIDE.md # Детальна інструкція з деплою
```

## Тестування

### В браузері
Відкрийте `index.html` в браузері для перевірки інтерфейсу.

### В Telegram
1. Додайте Web App в бот
2. Відправте команду що відкриває Web App
3. Перевірте що суми рахуються правильно
4. Перевірте редірект на Buy Me a Coffee

## Налаштування курсу UAH/USD

За замовчуванням використовується курс 1 USD = 37 UAH.

Щоб змінити, відредагуйте в `script.js`:

```javascript
const usdRate = 37; // Змініть на актуальний курс
```

## Підтримка

Якщо виникли проблеми, перевірте:
- ✅ GitHub Pages активований
- ✅ `bmcUsername` вказано правильно
- ✅ URL коректний (`https://`)
- ✅ Файли завантажені повністю

## Ліцензія

MIT License
