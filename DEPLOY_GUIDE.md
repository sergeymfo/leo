# 🚀 Інструкція з деплою Leo Donation Web App

## Крок 1: Підготовка репозиторію на GitHub

### Варіант A: Через веб-інтерфейс GitHub

1. Відкрийте https://github.com/sergeymfo/leo
2. Завантажте всі файли з папки `leo-donation-app`:
   - `index.html`
   - `styles.css`
   - `script.js`
   - `avatar.svg`
   - `README.md`
   - `.gitignore`
   - `bot_example.py`

3. Натисніть "Add file" → "Upload files"
4. Перетягніть файли
5. Напишіть commit message: "Add Leo Donation Web App"
6. Натисніть "Commit changes"

### Варіант B: Через командний рядок (Git)

```bash
# 1. Клонуйте репозиторій
git clone https://github.com/sergeymfo/leo.git
cd leo

# 2. Скопіюйте файли проекту в репозиторій
# (розпакуйте leo-donation-app.tar.gz і скопіюйте файли)

# 3. Додайте файли в git
git add .

# 4. Зробіть commit
git commit -m "Add Leo Donation Web App"

# 5. Запуште на GitHub
git push origin main
```

---

## Крок 2: Налаштування GitHub Pages

1. Відкрийте репозиторій: https://github.com/sergeymfo/leo

2. Перейдіть в **Settings** (праворуч вгорі)

3. В лівому меню знайдіть **Pages**

4. Налаштуйте деплой:
   - **Source**: Deploy from a branch
   - **Branch**: `main` (або `master`)
   - **Folder**: `/ (root)`
   - Натисніть **Save**

5. Зачекайте 1-2 хвилини

6. Ваш сайт буде доступний за адресою:
   ```
   https://sergeymfo.github.io/leo/
   ```

7. Перевірте чи працює сайт - відкрийте URL в браузері

---

## Крок 3: Налаштування Buy Me a Coffee

1. Зареєструйтесь на https://www.buymeacoffee.com/

2. Виберіть username (наприклад: `serhii` або `leoproject`)

3. Налаштуйте профіль:
   - Завантажте аватар
   - Напишіть опис
   - Встановіть валюту (USD, EUR або UAH якщо доступно)

4. **Важливо!** Встановіть базову ціну:
   - Перейдіть в **Settings → Profile**
   - Знайдіть **"Price per coffee"**
   - Встановіть **$1** (або еквівалент в іншій валюті)
   - Це важливо для правильного розрахунку сум!

5. Підключіть спосіб виведення коштів:
   - **Settings → Payouts**
   - Виберіть **Stripe** (рекомендовано) або **PayPal**
   - Заповніть дані для виведення

---

## Крок 4: Оновлення коду Web App

1. Відредагуйте файл `script.js` на GitHub:

2. Знайдіть розділ `CONFIG` (близько 5-го рядка):

```javascript
const CONFIG = {
    bmcUsername: 'serhii', // ⬅️ ЗАМІНІТЬ на ваш username
    basePrice: 1,          // Базова ціна на BMC
    minAmount: 10,
    maxAmount: 10000,
    currency: 'UAH'        // Або USD, EUR
};
```

3. Замініть `'serhii'` на ваш username з Buy Me a Coffee

4. Збережіть зміни (Commit changes)

5. GitHub Pages автоматично оновиться за 1-2 хвилини

---

## Крок 5: Тестування Web App

### Тест 1: Відкрити в браузері

1. Відкрийте `https://sergeymfo.github.io/leo/`
2. Перевірте чи завантажується сторінка
3. Спробуйте вибрати суму
4. Натисніть "Підтримати проект"
5. Має відкритись Buy Me a Coffee з правильною сумою

### Тест 2: Через Telegram Web App Tester

1. Відкрийте https://web.telegram.org/a/
2. Знайдіть @WebAppBot або @BotFather
3. Відправте команду з вашим URL:
   ```
   /start https://sergeymfo.github.io/leo/
   ```

---

## Крок 6: Інтеграція в Telegram бота

### Варіант 1: Inline кнопка

Додайте в код вашого бота:

```python
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton, WebAppInfo

WEBAPP_URL = "https://sergeymfo.github.io/leo/"

@router.message(Command("donate"))
async def cmd_donate(message: Message):
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(
            text="💰 Підтримати Leo",
            web_app=WebAppInfo(url=WEBAPP_URL)
        )]
    ])

    await message.answer(
        "💙 Підтримайте проект!\n\n"
        "Натисніть кнопку щоб обрати суму:",
        reply_markup=keyboard
    )
```

### Варіант 2: Reply keyboard

```python
from aiogram.types import ReplyKeyboardMarkup, KeyboardButton, WebAppInfo

keyboard = ReplyKeyboardMarkup(
    keyboard=[
        [KeyboardButton(
            text="💝 Підтримати",
            web_app=WebAppInfo(url=WEBAPP_URL)
        )]
    ],
    resize_keyboard=True
)
```

### Варіант 3: Меню бота (через @BotFather)

1. Знайдіть @BotFather в Telegram
2. Відправте `/setmenubutton`
3. Виберіть ваш бот
4. Натисніть "Edit menu button"
5. Введіть текст: `💰 Підтримати`
6. Введіть URL: `https://sergeymfo.github.io/leo/`
7. Готово! Кнопка з'явиться в меню бота

---

## Крок 7: Налаштування Webhooks (опціонально)

Щоб автоматично давати бонуси після донату:

### 1. На Buy Me a Coffee:

1. Перейдіть в **Settings → Integrations → Webhooks**
2. Натисніть **Add Webhook**
3. Введіть URL вашого сервера: `https://yourserver.com/bmc-webhook`
4. Виберіть події: "New supporter", "New membership"
5. Збережіть

### 2. На вашому сервері:

Додайте endpoint (приклад є в файлі `bot_example.py`):

```python
@app.route('/bmc-webhook', methods=['POST'])
async def bmc_webhook():
    data = request.json

    supporter_email = data.get('supporter_email')
    amount = data.get('support_coffee_price')

    # Знайти юзера і додати бонус
    user = await db.get_user_by_email(supporter_email)
    if user:
        bonus = int(amount) * 100
        await db.add_balance(user.telegram_id, bonus)
        await bot.send_message(
            user.telegram_id,
            f"🎉 Дякую за донат! Зараховано {bonus} кредитів!"
        )

    return {'status': 'success'}
```

---

## 📱 Фінальна перевірка

1. ✅ Web App доступний за URL
2. ✅ Buy Me a Coffee налаштований
3. ✅ Username вказаний правильно в `script.js`
4. ✅ Кнопка в боті відкриває Web App
5. ✅ Вибір суми працює
6. ✅ Редірект на BMC працює з правильною сумою
7. ✅ Оплата проходить успішно

---

## 🐛 Вирішення проблем

### Проблема: Web App не відкривається

**Рішення:**
- Перевірте чи включений GitHub Pages
- Перевірте URL (має бути `https://`, не `http://`)
- Зачекайте 2-3 хвилини після змін

### Проблема: Неправильна сума на BMC

**Рішення:**
- Перевірте базову ціну на BMC (має бути $1)
- Перевірте `basePrice` в `script.js`
- Формула: BMC сума = basePrice × amount

### Проблема: Кнопка не з'являється в боті

**Рішення:**
- Перевірте чи URL правильний
- Перевірте чи використовуєте `WebAppInfo`
- Перезапустіть бота

### Проблема: "This site can't be reached"

**Рішення:**
- Перевірте URL (скопіюйте точно з GitHub Pages)
- Перевірте чи файли завантажені в репозиторій
- Почистіть кеш браузера

---

## 🎨 Кастомізація

### Зміна кольорів:

Відредагуйте `styles.css`:

```css
:root {
    --primary-color: #5865F2;     /* ваш колір */
    --secondary-color: #4752C4;   /* ваш колір */
}
```

### Зміна сум:

Відредагуйте `index.html`:

```html
<button class="amount-button" data-amount="50">   <!-- змініть суму -->
<button class="amount-button" data-amount="100">
<button class="amount-button" data-amount="250">
```

### Зміна аватара:

Замініть `avatar.svg` на своє зображення або використайте PNG:

```html
<img src="your-avatar.png" alt="Leo">
```

---

## ✅ Готово!

Ваш Leo Donation Web App готовий до використання! 🎉

Якщо виникли питання - перечитайте відповідний розділ інструкції.

---

**Корисні посилання:**

- Ваш Web App: https://sergeymfo.github.io/leo/
- Buy Me a Coffee: https://www.buymeacoffee.com/
- GitHub Pages Docs: https://docs.github.com/pages
- Telegram Web Apps: https://core.telegram.org/bots/webapps

---

Бажаємо успіху! 🚀
