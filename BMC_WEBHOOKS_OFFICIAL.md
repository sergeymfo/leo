# 🔔 Buy Me a Coffee Webhooks - Офіційна документація

## ✅ Підтвердження офіційної підтримки

Buy Me a Coffee **офіційно підтримує webhooks**!

### 📚 Офіційні ресурси:
- **Документація:** https://studio.buymeacoffee.com/webhooks/docs
- **API Portal:** https://developers.buymeacoffee.com/
- **Налаштування:** https://www.buymeacoffee.com/webhooks
- **FAQ:** https://help.buymeacoffee.com/en/articles/8210728-faq-on-webhooks

---

## 🎯 Підтримувані події (Events)

Buy Me a Coffee відправляє webhooks для наступних подій:

### 1. **One-time Support (Donations)**
- ☕ Разова підтримка (coffee purchase)
- 💰 Повернення коштів (refunds)

### 2. **Extras (Products)**
- 🎁 Покупка extras (ваш випадок!)
- 💰 Повернення extras

### 3. **Memberships (Subscriptions)**
- 👥 Нова підписка
- 📊 Зміна рівня підписки
- ❌ Скасування підписки

---

## 📦 Формат Webhook Payload

### Офіційна структура JSON:

```json
{
  "response": {
    "supporter_name": "John Doe",
    "supporter_email": "supporter@example.com",
    "support_note": "Thanks for your work!",
    "number_of_coffees": "2",
    "total_amount": "10.00",
    "support_created_on": "2021-08-01 16:00:00"
  }
}
```

### Опис полів:

| Поле | Тип | Опис |
|------|-----|------|
| `supporter_name` | string | Ім'я підтримувача |
| `supporter_email` | string | Email підтримувача |
| `support_note` | string | Повідомлення від підтримувача (опціонально) |
| `number_of_coffees` | string | Кількість "кав" (для extras завжди кратно ціні) |
| `total_amount` | string | **ЗАГАЛЬНА СУМА в доларах** (готова до використання!) |
| `support_created_on` | string | Дата і час підтримки (YYYY-MM-DD HH:MM:SS) |

### ⚠️ Важливо:
- Всі дані знаходяться в об'єкті `response`
- `total_amount` вже містить **фінальну суму** - не потрібно нічого рахувати
- Для extras поле `number_of_coffees` може бути "1" або більше

---

## 🔐 Безпека (Security)

### X-BMC-Signature Header

Buy Me a Coffee **підписує кожен webhook** для захисту від підробок:

```http
X-BMC-Signature: f8a3d2c1b9e7a6d5c4b3a2f1e9d8c7b6a5f4e3d2c1b9a8f7e6d5c4b3a2
```

### Як перевірити підпис:

```python
import hmac
import hashlib

# 1. Отримати SECRET KEY з BMC webhook settings
webhook_secret = "your_secret_key_from_bmc"

# 2. Отримати body запиту (raw bytes)
body = await request.read()

# 3. Створити HMAC SHA256 підпис
expected_signature = hmac.new(
    webhook_secret.encode('utf-8'),
    body,
    hashlib.sha256
).hexdigest()

# 4. Порівняти з X-BMC-Signature header
signature = request.headers.get('X-BMC-Signature', '')
if not hmac.compare_digest(signature, expected_signature):
    # ПІДПИС НЕВІРНИЙ - відхилити запит!
    return error_response()
```

### 🛡️ Чому це важливо:
- Захист від підроблених запитів
- Гарантія що дані від BMC, а не від зловмисника
- **ЗАВЖДИ** перевіряйте підпис в production!

---

## ⚙️ Налаштування Webhook

### Крок 1: Створити Webhook

1. Перейти на https://www.buymeacoffee.com/webhooks
2. Натиснути **"Create New Webhook"**
3. Заповнити форму:
   - **Webhook URL:** https://your-domain.com/bmc-webhook
   - **Events:** Вибрати події (Purchases, Extras, Memberships)
4. Зберегти **SECRET KEY** (буде показано один раз!)

### Крок 2: Налаштувати сервер

```python
from aiohttp import web
import json

async def bmc_webhook_handler(request):
    # Перевірити підпис
    # Обробити дані
    # Повернути 200 OK
    return web.Response(status=200)

app = web.Application()
app.router.add_post('/bmc-webhook', bmc_webhook_handler)
```

### Крок 3: Відповісти швидко!

⚠️ **КРИТИЧНО:** Ваш endpoint повинен:
- Відповісти **2xx статусом** протягом кількох секунд
- Не виконувати складні операції в обробнику
- Якщо відповідь не прийде швидко → BMC позначить webhook як failed

### ✅ Правильний підхід:

```python
async def bmc_webhook_handler(request):
    data = await request.json()

    # 1. ШВИДКО ВІДПОВІСТИ
    response = web.Response(status=200)

    # 2. ПОТІМ обробити в фоні
    asyncio.create_task(process_payment_async(data))

    return response
```

---

## 🧪 Тестування Webhooks

### 1. Тестовий webhook від BMC

В webhook dashboard є кнопка **"Send Test"**:
- Натиснути → відкриється модальне вікно
- Показує **структуру JSON** для різних типів подій
- Можна відправити тестовий запит на ваш сервер

### 2. Локальне тестування

Для локального dev використовуйте **ngrok**:

```bash
# Запустити ngrok
ngrok http 8080

# Отримаєте URL типу: https://abc123.ngrok.io
# Використати в BMC як webhook URL: https://abc123.ngrok.io/bmc-webhook
```

### 3. Перевірка логів

BMC показує історію webhooks:
- ✅ Успішні (200 OK)
- ❌ Невдалі (timeout, 4xx, 5xx)
- 🔄 Повторні спроби (retries)

---

## 🎯 Ваш Use Case: Extras для Leo Donation

### Як працює ваша система:

1. **Користувач натискає "Підтримати $10"**
   - Web App відкриває: https://buymeacoffee.com/fwdr/e/477543
   - Зберігає pending payment в БД (amount=$10, timestamp=now)

2. **Користувач оплачує на BMC**
   - BMC обробляє платіж
   - Відправляє webhook на ваш сервер

3. **Ваш сервер отримує webhook:**
   ```json
   {
     "response": {
       "supporter_name": "Ivan",
       "supporter_email": "ivan@example.com",
       "total_amount": "10.00",
       "support_created_on": "2025-11-10 14:30:00"
     }
   }
   ```

4. **Автоматичне зіставлення:**
   - Шукає pending payment з amount=$10 за останні 30 хв (FIFO)
   - Знаходить → оновлює статус → зараховує бонуси
   - Відправляє повідомлення користувачу в Telegram

### ✅ Переваги цього підходу:
- Користувачу не потрібно вводити Payment ID
- Автоматичне зарахування
- Працює для фіксованих сум ($5, $10, $25, $50, $100)

### ⚠️ Обмеження:
- Якщо 2 користувачі оплачують $10 одночасно → перший в черзі отримає (FIFO)
- Працює тільки з extras (фіксовані суми)
- Вікно збігу 30 хвилин (можна налаштувати)

---

## 📝 Чеклист для Production

- [ ] Webhook URL налаштовано в BMC dashboard
- [ ] SECRET KEY збережено в `.env` файлі (не в коді!)
- [ ] Перевірка X-BMC-Signature реалізована
- [ ] Endpoint відповідає протягом 2-3 секунд
- [ ] Складна обробка винесена в background tasks
- [ ] Логування webhooks налаштовано
- [ ] Тестовий webhook успішно отриманий
- [ ] HTTPS налаштовано (BMC вимагає SSL)
- [ ] Database готова для pending_payments

---

## 🔗 Корисні посилання

- **Створити webhook:** https://www.buymeacoffee.com/webhooks
- **API Documentation:** https://developers.buymeacoffee.com/
- **FAQ про webhooks:** https://help.buymeacoffee.com/en/articles/8210728-faq-on-webhooks
- **Інтеграції (Zapier, Pipedream):** https://help.buymeacoffee.com/en/collections/4079507-integrations

---

## 💡 Додаткові можливості

### 1. Множинні webhooks
Можна створити кілька webhooks для різних цілей:
- Один для production bot
- Один для analytics
- Один для backup/logging

### 2. Різні події
Налаштувати різні URL для різних подій:
- `/bmc-purchases` → для extras
- `/bmc-memberships` → для підписок
- `/bmc-refunds` → для повернень

### 3. Інтеграція з іншими сервісами
- **Discord** - повідомлення про нові підтримки
- **Slack** - алерти для команди
- **Google Sheets** - автоматичний облік
- **Analytics** - трекінг конверсій

---

## ❓ FAQ

**Q: Чи можна отримати історію всіх платежів через API?**
A: Так! Buy Me a Coffee має API для отримання списку supporters та payments.

**Q: Що якщо webhook не спрацює?**
A: BMC автоматично повторює спроби (retries). Також можна отримати дані через API.

**Q: Скільки часу чекати на webhook?**
A: Зазвичай 1-5 секунд після оплати. Максимум до 1 хвилини.

**Q: Чи підтримується webhook для refunds?**
A: Так! Окрема подія для повернень коштів.

---

✅ **Ваш webhook handler (`bmc_webhook_handler.py`) тепер використовує офіційний формат BMC!**
